# 「智景 ScenicMind」经营分析 Agent 架构设计

> 对应 PRD F3「特征贡献度分析与 Agent 报告」。定义 Agent 的分层架构、意图路由、工具层与证据约束输出规范。
> 当前落地为**证据约束型规则 Agent**（不依赖外部 LLM 即可稳定运行），表达层预留 LLM 接入点。

---

## 1. 设计目标与定位

Agent 的定位是「AI 经营分析师」：不只回答"客流多少"，还回答"为什么"和"怎么办"，且**每条结论都能溯源到数值或证据**。

三个硬约束（贯穿全部实现）：

1. **证据约束（防幻觉）**：任何归因结论必须挂数值依据（SHAP/特征贡献），任何建议必须挂证据编号或知识库条目；数值不支持的结论一律删除。
2. **诚实降级**：数据不足时明示"样本不足，置信度低"，宁可不答也不编造。
3. **只建议不执行**：输出均为建议态，无直连闸机/票务的写通道。

---

## 2. 分层架构

```
┌──────────────────────────────────────────────────────────────┐
│  接入层   HTTP POST /api/v1/agent/chat                         │
│           （message + spot + sessionId，支持多轮）             │
├──────────────────────────────────────────────────────────────┤
│  编排层   Orchestrator（orchestrator.py）                       │
│           ┌────────────┐  ┌─────────────┐  ┌──────────────┐   │
│           │ 意图路由    │→ │ 工具调度      │→ │ 回答组装       │   │
│           │ Router     │  │ Dispatcher  │  │ Responder     │   │
│           └────────────┘  └─────────────┘  └──────────────┘   │
│                ↓                ↓                 ↓           │
│           规则路由(可升级LLM)  工具结果归一化   模板+LLM预留     │
├──────────────────────────────────────────────────────────────┤
│  工具层   Tools（tools.py）—— 全部只读、返回结构化数据           │
│   query_forecast   客流预测（复用模块一 build_forecast）         │
│   query_report     经营报告（复用模块二 build_report）           │
│   query_drivers    归因/特征贡献                                │
│   query_evidence   游客原声（证据编号 + 影响分 + 原文链接）       │
│   whatif           反事实推理（对预测做规则化场景调整）          │
├──────────────────────────────────────────────────────────────┤
│  知识层   Knowledge（knowledge.py）—— 内置运营知识库             │
│   · 经营建议模板（客流组织/天气响应/交通接驳/游客体验/服务）      │
│   · 风险阈值与话术（承载率 75%/85%/90% 分级）                    │
│   · 行业基准（排队时长、二消、错峰等）                            │
├──────────────────────────────────────────────────────────────┤
│  表达层   Responder（responder.py）                             │
│   · 默认：规则模板（结构化数据 → 自然语言）                      │
│   · 预留：LLM 生成接口（结构化 Prompt + 校验层，接入后无缝替换）  │
└──────────────────────────────────────────────────────────────┘
```

**分层原则**：
- 工具层与知识层**只读**，输出结构化数据，不含表达；表达层负责组织语言。
- 意图路由与回答组装解耦：路由决定"调用哪些工具"，组装决定"怎么说"。
- LLM 是表达层的**可插拔实现**，不是 Agent 的骨架——骨架是证据约束。

---

## 3. 意图路由（Intent Router）

优先规则匹配（关键词 + 正则），预留 LLM 语义路由。当前支持的意图：

| 意图 | 触发示例 | 工具组合 | 回答形态 |
|------|---------|---------|---------|
| `data_query` | 今天多少人 / 峰值哪天 / 未来7天 | query_forecast + query_report | 数值摘要 + 趋势 |
| `attribution` | 为什么高/低 / 什么原因 | query_drivers + query_report | 归因 + 特征贡献数值 |
| `recommendation` | 该怎么办 / 建议 / 怎么应对 | query_report + knowledge | 建议 + 证据编号 + 优先级 |
| `whatif` | 如果下雨 / 如果人数翻倍 / 万一 | whatif + query_forecast | 对比 + 风险提示 |
| `evidence` | 游客怎么说 / 评论 / 投诉 | query_evidence | 原声 + 影响分 + 链接 |
| `greeting` / `fallback` | 你好 / 其他 | — | 引导 + 追问建议 |

路由失败或识别为闲聊时进入 `fallback`，返回能力清单与可追问的示例。

---

## 4. 对话协议

### 请求

```json
{
  "message": "如果下周六下雨，客流会怎样？",
  "spot": "黄果树瀑布",
  "sessionId": "optional-session-id"
}
```

### 响应

```json
{
  "reply": "若下周六（09月04日）降雨概率升高，预计客流从 47,600 人下调约 15%，至 40,000 人左右……",
  "intent": "whatif",
  "spot": "黄果树瀑布",
  "evidence": [
    { "type": "driver", "label": "降雨概率", "value": "-2,800 人", "ref": "DRIVER-RAIN" },
    { "type": "voice", "id": "VOICE-03", "quote": "……", "sourceUrl": "https://..." }
  ],
  "suggestions": ["本周峰值会到承载量的多少？", "给出应对下雨的运营建议"],
  "trace": {
    "agentVersion": "scenicmind-agent-v1",
    "intentSource": "rule-router",
    "generationMode": "rule-template",
    "evidenceBound": true
  }
}
```

- `reply`：给用户看的自然语言回答。
- `evidence`：回答所依赖的数值/证据依据，前端可展开查看（防"AI 瞎说"）。
- `suggestions`：可追问的引导，驱动多轮对话。
- `trace`：生成模式与版本，可回放。

---

## 5. 证据约束实现（三道闸）

1. **数值闸**：`attribution` 类回答中的每个"因为 X"，必须能映射到 `drivers[].contributionVisitors` 或 `kpis` 中的数值；映射不到的结论不生成。
2. **出处闸**：`recommendation` 类回答中的每条建议，必须携带 `evidenceRefs`（游客原声编号）或知识库条目 ID；否则标注"模型推断（无直接证据）"。
3. **降级闸**：`query_evidence` 样本数 < 阈值时，回答首句明示"当前该景点评论样本不足，结论置信度低"。

---

## 6. 多轮对话记忆

- `sessionId` 为可选；传入时服务端维护轻量会话上下文（最近 N 轮意图与关键参数），使"如果下雨呢"这类省略指代能解析到上一轮的景点/日期。
- MVP 阶段会话记忆为进程内 LRU 缓存（不落库）；产品化后替换为持久化会话存储。

---

## 7. LLM 表达层预留

`responder.py` 定义统一接口：

```python
def render(intent, context) -> ChatResponse
```

- 默认实现：`RuleRenderer`（规则模板 + 数据插值，无外部依赖）。
- 预留实现：`LLMRenderer`——将 `context`（结构化数据 + 约束提示词 + 校验要求）发给 LLM，返回前经过同一套证据校验层。切换只改一处装配，不影响编排与工具。

---

## 8. 边界与不做什么

- 不做自由生成式闲聊，what-if 是**受限场景反事实**（天气/人数/日期），不开放任意代码执行。
- 不接入票务/闸机写接口，Agent 输出永远是建议态。
- 不把 LLM 作为数据来源，LLM 只做表达层，数据永远来自工具层。
