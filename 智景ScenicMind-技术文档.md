# 「智景 ScenicMind」景区智能平台 技术文档

> 对应 PRD V1.0.0。覆盖 MVP 三件套（预测引擎 / 数据看板 / 特征贡献度与 Agent 报告）与 V1 排班优化的技术实现。

---

## 1. 总体技术路线

| 层 | 技术选型 | 说明 |
|----|---------|------|
| 数据接入 | Python 爬虫 + REST API + Excel 导入管道 | 闸机/票务接口、天气 API、手工导入三通道 |
| 存储与计算 | PostgreSQL（业务）+ TimescaleDB（时序）| 单景区数据量小，一体化部署可行 |
| 预测模型 | PyTorch：TFT（Temporal Fusion Transformer）为主 + LightGBM 基线 | 分层模型策略（见 3.2） |
| 可解释性 | SHAP（TreeExplainer + DeepExplainer/Integrated Gradients） | 全局 + 局部特征贡献度 |
| Agent 报告 | LLM API + 结构化 Prompt + RAG（运营知识库） | 报告生成与 what-if 对话 |
| 排班优化（V1） | OR-Tools CP-SAT 求解器 | 约束满足 + 成本目标 |
| 应用层 | React + ECharts + FastAPI | 看板与报告 Web 端 |
| MLOps | MLflow（实验追踪）+ Airflow（调度）+ 模型版本快照 | 每周自动重训练 |

## 2. 数据层设计

### 2.1 数据源

| 数据 | 来源 | 粒度 | 频率 |
|------|------|------|------|
| 历史客流 | 闸机通行记录（首选）/ 票务核销 | 15 分钟~小时 | 实时或 T+1 |
| 天气 | 气象 API + 分钟级雷达（后续） | 小时 | 每小时 |
| 节假日日历 | 内置中国节假日库（含调休、寒暑假、地方节庆） | 日 | 静态+年度更新 |
| 预约数据 | 景区预约系统（可得时） | 分时段 | 实时 |
| 事件标签 | 手工登记（演唱会/赛事/免票活动） | 日 | 人工 |

### 2.2 数据管道

```
原始数据 → 清洗（缺失/异常/重复）→ 统一时序对齐（景区-区域-时段）
   → 特征库（见 3.1）→ 训练集/推理请求 → 模型
```

关键清洗规则：闸机双向去重（入园-出园配对）、极端异常日打标（免票日/闭园日）单独处理而非直接剔除（它们本身就是高价值特征）。

## 3. 客流预测引擎（F1）

### 3.1 特征体系

| 特征组 | 特征 | 说明 |
|--------|------|------|
| 时间结构 | 小时/星期/月、节假日类型（法定假/调休/周末/寒暑假/工作日）、节前节后偏移（节前 1 天/节后 1 天） | 客流周期骨架 |
| 外生环境 | 温度、降雨量/降雨概率、极端天气标记 | 解释同日偏差 |
| 票务先行 | 预约量、预约消耗速率、OTA 搜索热度（可得时） | 短期最强信号 |
| 滞后特征 | 前 7 天/14 天/28 天同期客流、前一日同时段、在场人数 | 水平锚定 |
| 事件特征 | 闭园/免票/大型活动哑变量 | 尾部事件 |

### 3.2 分层模型策略（关键工程决策）

**"内置神经网络"是产品能力，不是教条——小样本下 NN 劣于树模型，必须自动选优：**

| 数据量 | 模型 | 理由 |
|--------|------|------|
| < 3 个月 | 统计基线（同期均值 × 节假日系数 × 天气调整） | 冷启动，零训练成本，透明 |
| 3-12 个月 | LightGBM 分位回归 | 小样本稳健，特征重要性天然可解释 |
| ≥ 12 个月 | TFT（神经网络主干） | 多变量长依赖 + 注意力机制可输出分位区间 |

**自动选优器**：每月对全部候选模型做滚动回测（过去 60 天），选 MAPE 最低者上线，其余降为影子模型。产品话术保持"内置智能预测引擎"，工程实现是分层选优——这是诚实的工程与营销的平衡点。

### 3.3 训练与推理管道

- **训练**：Airflow 每周任务——拉取增量数据 → 特征计算 → 重训练 → 回测评估（MLflow 记录）→ 优于线上模型才发布（灰度：新旧模型并行预测一周）
- **推理**：每日 5:00 批量生成未来 7 天日级 + 3 天分时段预测；当日每小时滚动更新（接入实时闸机数据后）
- **输出**：P50/P90 分位区间 + 预测失败告警（连续偏差 >40% 触发自诊断：特征漂移检测）

### 3.4 评估指标

| 指标 | 目标 |
|------|------|
| 日级 MAPE | ≤ 25%（MVP）→ ≤ 20%（V1） |
| 分时段 MAPE | ≤ 35% |
| 分位覆盖率 | P90 实际覆盖 ≥ 85%（区间可信度） |
| 节假日单独 MAPE | ≤ 30%（节假日是景区最在乎的时段） |

## 4. 数据看板（F2）

- **架构**：FastAPI 聚合层（预计算物化视图）+ React/ECharts 前端
- **核心图表**：实际 vs 预测双线图（含置信带）、逐小时热力图（日期×小时）、同比/环比柱状、偏差瀑布图
- **性能**：看板数据走预计算（小时级物化），查询 P99 ≤ 2s
- **设计原则**：预测偏差可视化是续费生命线——不掩饰模型错误，用透明换信任

## 5. 特征贡献度与 Agent 报告（F3，差异化核心）

### 5.1 可解释性计算

| 层级 | 方法 | 输出 |
|------|------|------|
| 全局贡献度 | SHAP summary（TreeSHAP / DeepSHAP） | 特征重要性排名：哪些因素长期驱动该景区客流 |
| 单日局部贡献度 | SHAP waterfall | "今日预测客流较上周 -18%：降雨 -12%、工作日 -5%、前期降温 +2%、其余 +1%" |
| what-if | 反事实推理（改动输入特征重跑模型） | "若降雨概率降为 20%，客流回升至 X" |

**工程要点**：NN 用 DeepSHAP/Integrated Gradients 一次前向计算贡献度，推理延迟 <1s，随每日报表批量预计算为主、交互 what-if 实时算为辅。

### 5.2 Agent 报告生成

```
输入组装（结构化）：
  ① 本周预测 vs 实际复盘（含 MAPE）
  ② SHAP 归因摘要（全局 + 波动日局部）
  ③ 景区运营知识库检索（RAG：整改案例、行业基准、
     该景区历史采纳记录）
  ④ 约束与风格（建议必须具体、量化、挂数值依据）
        ↓ LLM 生成（结构化 Prompt，分节生成）
周报：复盘 → 归因 → 3-5 条经营建议 → 风险提示（承载量预警）
        ↓
校验层：归因句与 SHAP 数值一致性核对（防幻觉），
       建议与知识库出处挂钩
```

**防幻觉三道闸**：①归因结论必须能映射到 SHAP 数值（数值不支持的归因删除）；②建议必须引用知识库条目或明确标注"模型推断"；③样本不足场景强制降级为"置信度低"模板。

### 5.3 what-if 对话

用户问"如果下周六下雨呢"→ Agent 解析 → 修改天气特征重跑推理 → 返回对比。本质是受限参数的反事实查询，不开放自由生成。

## 6. 排班优化（F4，V1）

- **问题建模**：约束满足 + 线性目标
  - 变量：岗位 × 时段 × 员工的 0/1 分配矩阵
  - 硬约束：班次规则（最短班/间隔）、技能匹配、劳动法规、员工可用性
  - 软约束/目标：最小化"人力缺口小时数"（预测需求 − 排班供给在阈值下的欠量）+ 加班成本
- **求解器**：OR-Tools CP-SAT（中小规模景区 <10 万变量，秒级求解）
- **输出**：排班表 + 覆盖率曲线（预测需求 vs 排班供给逐时段对比）+ 成本对比
- **人机边界**：只生成建议方案，排班确认权在运营负责人

## 7. 关键技术风险与对策

| 风险 | 对策 |
|------|------|
| 闸机数据质量差（双向计数、断连） | 清洗规则库 + 数据质量评分（评分低于阈值自动降级为基线模型并在看板明示） |
| NN 小样本劣化 | 分层自动选优（3.2），永不强行上 NN |
| SHAP 计算成本 | 批量预计算 + 缓存；交互场景仅算局部贡献 |
| LLM 归因幻觉 | 三道闸校验（5.2） |
| 多景区差异大 | 特征工程框架统一 + 景区级模型实例隔离，避免互相污染 |
