# 「智景 ScenicMind」景区智能平台 架构文档

> 对应 PRD V1.0.0 与技术文档。定义系统分层架构、数据流、部署形态与接口规范。

---

## 1. 架构总览（分层）

```
┌─────────────────────────────────────────────────────────────┐
│  端层    Web 看板（运营/管理层）   报告中心   管理后台          │
├─────────────────────────────────────────────────────────────┤
│  应用层  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│          │ 看板服务  │ │ 报告服务  │ │ 排班服务  │ │ 告警   │ │
│          │ (FastAPI)│ │(Agent编排)│ │(V1/求解) │ │        │ │
│          └──────────┘ └──────────┘ └──────────┘ └────────┘ │
├─────────────────────────────────────────────────────────────┤
│  AI 引擎层                                                   │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌─────────────┐ │
│  │ 预测引擎   │ │ 可解释引擎  │ │ 报告 Agent │ │ 排班求解器   │ │
│  │ TFT/GBM   │ │ SHAP      │ │ LLM+RAG   │ │ OR-Tools   │ │
│  │ 自动选优   │ │ 全局/局部  │ │ 防幻觉校验 │ │ CP-SAT     │ │
│  └───────────┘ └───────────┘ └───────────┘ └─────────────┘ │
│                     MLOps：MLflow + Airflow + 模型注册       │
├─────────────────────────────────────────────────────────────┤
│  数据层    TimescaleDB（时序客流）│ PostgreSQL（业务/报告）     │
│            特征库（物化）│ 知识库（RAG 向量库）                 │
├─────────────────────────────────────────────────────────────┤
│  接入层    闸机/票务 API │ 天气 API │ Excel 导入 │ 预约系统接口  │
└─────────────────────────────────────────────────────────────┘
```

**架构原则**：
1. **模型即服务、景区即租户**：每个景区独立的模型实例与数据 schema（隔离防污染），共享一套引擎代码
2. **预测与解释同源**：可解释引擎直接消费预测引擎的模型对象与中间结果，不重复训练
3. **AI 只建议不执行**：应用层所有输出均为建议态，无直连闸机/票务的写接口（安全与责任边界）

## 2. 核心数据流

### 2.1 日常预测流（每日）

```
05:00  Airflow 触发
  → 拉取昨日闸机数据 + 天气 + 预约 → 清洗对齐
  → 特征计算（特征库物化视图更新）
  → 推理：日级 7 天 + 分时段 3 天（P50/P90）
  → SHAP 预计算（周报素材）
  → 写入预测表 → 看板缓存刷新
```

### 2.2 周报生成流（每周一 07:30）

```
  → 汇总：本周预测复盘（MAPE）+ SHAP 归因 + 采纳记录
  → RAG 检索运营知识库
  → LLM 分节生成 → 校验层（SHAP 数值一致性核对）
  → 报告入库 → 08:00 推送（站内 + 邮件/企微）
```

### 2.3 交互流（what-if / 看板查询）

```
用户提问/操作 → 应用层 → 可解释引擎实时计算（局部 SHAP / 反事实推理）
  → Agent 组装回答 → 前端渲染（贡献度瀑布图等）
```

### 2.4 训练流（每周 + 触发式）

```
  → 增量数据 → 重训练候选模型（NN/GBM/基线）
  → 滚动回测（过去 60 天）→ MLflow 记录
  → 优于线上模型 → 灰度并行一周 → 切换（模型版本快照留存）
```

## 3. 部署架构

### 3.1 两种部署形态（产品化关键）

| 形态 | 适用 | 架构 |
|------|------|------|
| **云 SaaS 多租户** | 民营/集团景区、数据无敏感顾虑 | 单云多租户，K8s 容器化，按景区逻辑隔离 |
| **景区本地化部署** | 国有景区（数据不出园要求） | Docker Compose 一体机（应用+DB+模型推理），模型训练在云端、推理与数据留本地，仅同步加密的模型指标回云（可选） |

**混合策略**：训练在云（利用 GPU 弹性）、推理在端（满足数据合规）——"数据不动模型动"。本地版牺牲自动重训练频率（月度更新模型包下发），换取数据合规。

### 3.2 环境划分

dev / staging / prod 三环境；模型注册中心管理 dev→prod 的模型发布；本地化部署走"模型包签名下发"通道。

## 4. 接口规范（关键 API 摘要）

| 接口 | 方法/路径 | 说明 |
|------|----------|------|
| 预测查询 | GET /api/v1/forecast?spot={id}&range=7d | 返回逐时段 P50/P90 与模型版本号 |
| 实际客流上报 | POST /api/v1/flow/batch | 闸机/票务数据批量推送（也支持文件导入） |
| 看板聚合 | GET /api/v1/dashboard/overview | 预计算物化视图读取 |
| 特征贡献 | GET /api/v1/explain/local?date={d} | 单日 SHAP 瀑布数据 |
| what-if | POST /api/v1/explain/counterfactual | 修改特征子集重推理，返回对比 |
| 报告 | GET /api/v1/report/weekly / POST /api/v1/report/subscribe | 报告拉取与订阅 |
| 排班求解（V1） | POST /api/v1/scheduling/solve | 输入约束与预测，返回排班方案 |

统一约定：鉴权（JWT + 租户隔离）、幂等、审计日志全量留痕、错误码分级。

## 5. 非功能性架构决策

| 维度 | 决策 |
|------|------|
| 性能 | 看板读物化视图（写时计算）；推理批处理与在线分离 |
| 可用性 | 应用双实例；Airflow 任务失败重试与告警；看板降级为最近缓存 |
| 安全 | 租户级 RBAC；客流数据脱敏导出；本地化部署满足数据不出园；全接口审计 |
| 可追溯 | 每份报告记录生成时的模型版本 + 数据快照 + Prompt 版本——出问题可回放 |
| 扩展性 | 引擎层无状态（预测/解释/报告服务可水平扩）；新景区 = 新租户配置 + 数据接入，不改代码 |
| 成本 | 单景区数据量小（年千万级记录以内），PostgreSQL+TimescaleDB 足够；LLM 成本集中在周报生成（每景区每月约百次调用），可控 |

## 6. 演进路线（架构视角）

| 阶段 | 架构变化 |
|------|---------|
| MVP | 单景区单实例（甚至一台云主机），验证三件套 |
| V1 | 抽出多租户层 + 本地化部署包 + 排班服务接入 |
| V2 | 模型注册与灰度体系完善；多景区知识库沉淀（跨景区对标能力）；开放 API（预测能力输出） |

**反原则清单（架构层面明确不做）**：不做微服务全家桶（MVP 单体优先）、不做实时流式架构（小时批足够）、不接游客端流量（C 端另行立项）、不给 AI 直连执行的通道。
