[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scenicmind-backend"
version = "0.1.0"
description = "Minimal ScenicMind backend skeleton"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116,<1",
  "uvicorn[standard]>=0.35,<1",
]

[tool.setuptools.packages.find]
include = ["app*"]
